# Vite Frontend Starter

Современная сборка на Vite с поддержкой SCSS, линтеров, автоматической оптимизации изображений и SVG-спрайта.

---

## Требования к окружению

Проект стабильно работает в следующем окружении:

- **Node.js: 20.x (LTS)**  
- **npm** — версия, поставляемая вместе с Node.js 20

> ⚠️ Использование других версий Node.js (например, 16, 18 или 22+) может привести к ошибкам установки или сборки, а также к некорректной работе отдельных модулей и зависимостей.

## 📦 Установка

```
npm install
```

---

## ▶️ Запуск разработки

```
npm run start
```

Локальный сервер:  
http://localhost:5173

---

## Deploy

Проект предназначен для статического хостинга  
(GitHub Pages, Vercel, Netlify и аналоги).

---

## 🏗 Сборка проекта

```
npm run build
```

---

## 👀 Предпросмотр сборки

```
npm run preview
```

---

## 🖼 Оптимизация растровых изображений

Исходники (2× из Figma):

```
raw/img/
```

Запуск конвертации (создаёт 1×, 2×, webp):

```
npm run convert:images
```

Результат:

```
src/images/
```

---

## 🖌 Оптимизация SVG

### Одиночные SVG

Исходники:

```
raw/svg/inline/
```

Запуск оптимизации:

```
npm run optimize:svg
```

Результат:

```
src/images/*.svg
```

Использование в HTML:

```html
<img src="/src/images/icon-name.svg" alt="">
```

Использование в CSS:

```css
background-image: url("/src/images/icon-name.svg");
```

---

## 🧩 SVG-спрайт

Исходники для спрайта:

```
raw/svg/sprite/
  plus.svg
  arrow-right.svg
  ...
```

Сборка:

```
npm run sprite
```

Или полный запуск оптимизации SVG и создания спрайта:

```
npm run svg
```

Результат:

```
src/images/sprite/stack.svg
```

---

## 📌 Правила использования спрайта

Спрайт собирается из файлов:

```
raw/svg/sprite/
```

Итоговый файл находится здесь:

```
src/images/sprite/stack.svg
```

Вставка иконки в HTML:

```html
<svg class="icon icon--plus">
  <use href="/src/images/sprite/stack.svg#plus"></use>
</svg>
```

Использование иконки в CSS (mask-image):

```css
.icon {
  width: 24px;
  height: 24px;
  display: inline-block;
  background-color: currentColor;
  mask-size: cover;
  mask-repeat: no-repeat;
}

.icon--plus {
  mask-image: url("/src/images/sprite/stack.svg#plus");
}
```

---

## 🔍 Линтеры и проверки

### SCSS

```
npm run lint:styles
```

### JS

```
npm run lint:js
```

### HTML

```
npm run lint:html
npm run validate:html
npm run w3c
```

### Проверка структуры файлов

```
npm run ls-lint
```

---

## 📁 Фавиконы

Файлы кладём в:

```
public/
```

Vite автоматически копирует их при сборке.

## 🎨 Источники и вдохновение

Анимация фоновых линий выполнена по мотивам примера Ankit Jaiswal:  
https://codepen.io/ankit567/pen/vYOYqPG  

Код адаптирован и переработан под данный проект.